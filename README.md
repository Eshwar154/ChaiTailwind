# ChaiTailwind
ChaiTailwind is a lightweight, utility-first styling engine that brings the speed of Tailwind CSS to the web using a pure JavaScript runtime. By parsing specific class prefixes, it dynamically maps shorthand utility strings directly to element styles without the need for a heavy CSS build step
